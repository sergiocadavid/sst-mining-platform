{
  "name": "sst-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for SST backend",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "init-db": "wrangler d1 execute sst_db --local --file=./schema.sql"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240405.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.80.0"
  },
  "engines": {
    "node": ">=20.18.0"
  }
}
